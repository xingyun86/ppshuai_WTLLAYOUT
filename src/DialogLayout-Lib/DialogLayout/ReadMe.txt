========================================================================
    STATIC LIBRARY : DialogLayout Project Overview
========================================================================

The DialogLayout project is built as a static library, but if you wish, you could easily
just add the files to your project. You will just need to access the class CDialogLayout,
the others are only used internally.
